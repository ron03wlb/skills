@/Users/ron/.codex/RTK.md

使用繁體中文對話。Shell 指令一律以 `rtk` 執行；需要原始輸出時用 `rtk proxy`。

先理解要求與相關證據，再做能完成任務的最小變更。保留既有風格、他人工作與無關變更；只清理由本次修改造成的殘留。

在已授權範圍內自主完成例行、可逆且不改需求的選擇。只有答案會實質改變需求、契約、歸屬或新增不可逆權限時才提問；使用者明確要求先確認時，依其要求。已確認的範圍與授權持續有效，無變化時不重問。

使用最短可靠的路徑取得證據。先搜尋相關段落，按需讀取來源；只有可獨立產生價值的工作或需要獨立證據的 review 才委派。依風險選驗證與推理強度，不為例行工作建立額外流程。

一般變更在要求已達成、適用檢查通過、結果與限制已說明時完成。既有 workflow 的完成條件由各 owner 定義：execution 需 AC、當前 candidate 的必要驗證與 review，以及 completion read-back；closeout 需指定 target 可達、精確 worktree 清理及 tracker closure；Run 需所有必要節點與 parent 完成。Worker 結束或部分成功不代表整體完成。

規劃、修改、驗證、review、commit、整合、push、release 與 deploy 是不同證明狀態；只宣稱實際完成且已核對的狀態。依明確授權執行外部與不可逆操作。

需求或 scope 改變、授權或歸屬不明、需新增不可逆權限，或既有恢復預算耗盡且無進展時，保留成果並說明阻擋、證據與下一步。重試沿用既有修復預算。
