---
status: accepted
---

# Use Issue-native execution and closeout

Retain `execute-issue` and `close-issue` as an explicit alternative within Matt's existing Issue flow. Ordinary `to-spec` and `to-tickets` create the tracker work and `/implement` remains the default implementation step. When the user explicitly selects the worktree alternative, each dependency-ready Issue is implemented in its own worktree, reviewed for Standards and Spec, and repaired for at most ten waves by `execute-issue`; a separately invoked `close-issue` refreshes and fast-forwards the original local branch, removes the clean Issue worktree, and closes the Issue.

Before tracker work becomes executable, `to-spec` selects a Planning Seal commit as its baseline. A newly created seal contains only the approved planning-artifact changes; when no relevant delta exists, the current target commit is reused. Optional `to-tickets` validates the inherited seal and may create one successor only for exact approved in-Spec planning changes; a change to public behavior, acceptance, target, or exclusion returns to `to-spec`. An approved plan or conversation may skip `/to-spec` and invoke `/to-tickets` directly; in that path the approved breakdown is scope authority for a primary seal. `execute-issue` validates seal identity, ancestry, and currency but never creates or repairs the seal. This is Git baseline hygiene, not a return to hashed lifecycle authorization.

Remove `ask-ron`, `to-spec-ron`, `to-tickets-ron`, and their hashed contract and lifecycle-authorization envelopes. Direct invocation covers one local execution or closeout, a human serializes integrations per target branch, and a plain tracker completion note hands the reviewed candidate between the two skills. This gives up machine-verified authorization lineage in exchange for the smaller Issue-native flow while retaining clean-worktree checks, exact Git identities, separate Standards/Spec review, and exclusions for push, remote merge, deploy, and unrelated deletion. `/wiki` and `/remove-ron` remain independent controls.

Clean-worktree checks apply to the reviewed Issue candidate, not to the original target worktree as a whole. `close-issue` may fast-forward a target that contains unrelated staged, unstaged, or untracked work only after a read-back `PREPARED` completion-note receipt binds the target-before, candidate, canonical dirty digest, non-sensitive counts, and effective post-merge hook evidence. Candidate and dirty rename sets include both endpoints, and collision checks use NUL-safe repository/filesystem path semantics. Target, digest, or hook-evidence drift stops the invocation. A matching post-fast-forward digest promotes the receipt to read-back `VERIFIED`, while `FAILED` persists both expected and failure-time observed fields; matching `PREPARED` retries may recover only from the recorded target-before or candidate state, and every other receipt/target state stops by default. After a successful fast-forward with later dirty drift, a human who has seen the failure may explicitly accept a separate read-back `RECONCILED` receipt only when target/candidate identity, fast-forward reflog, clean exact worktree, original failure-time evidence, and zero collision are proved. A legacy receipt without transition-time hook evidence additionally requires current hook absence and explicit human acceptance that historical absence is not retroactively proven. `RECONCILED` states that preservation equality is not proven and never replaces the `FAILED` receipt. No path or file content enters either receipt, and no failure triggers automatic stash, commit, cleanup, refresh, rollback, or repair. Issue work may proceed in parallel, while integration is serialized only per target branch.
