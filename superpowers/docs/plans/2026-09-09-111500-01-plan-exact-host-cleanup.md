# Exact helper recovery for Issue closeout

**Goal:** Recover Windows helper-held, empty Issue directories and continue closeout without repeating completed implementation or weakening physical cleanup.
**Why planning is required:** Process termination and the closeout ownership contract are consequential host and workflow changes.
**Authority:** On 2026-09-09 the user requested diagnosis and repair of Issues 78 and 79, then explicitly chose to retain physical cleanup and add exact helper recovery. That authorizes this bounded implementation; no tracker publication, push or global installation is requested.
**Baseline:** `4d40439f1f50578b0d7ad836b044814c6b012022`, repository `ron03wlb/skills`, isolated branch `codex/fix-issue-78-79-host-cleanup-20260909`.
**Evidence:** The two selected original tasks are `01a08041-4b0e-7182-882a-95effb70219c` and `01a0807b-be7f-7d82-bf32-07ef587938ce`, observed through 2026-09-09T02:41:36Z. Their original session logs establish successful integration followed by Permission denied/EBUSY. Issue 79's later, separately authorized host repair released 15 identity-bound helper processes and reached SUCCEEDED. It did not implement reusable recovery. The current pending-host-cleanup helper only assesses and blocks. This is a focused two-task sample, not an exhaustive latest-50 retrospective.
**Acceptance:** Preserve the reviewed candidate, integration obligations, original task and deterministic close operation. Require an idle/completed exact task, ordinary empty unregistered exact directory, retained topic/candidate, clean target and candidate ancestry. Recover only supported Codex helper processes with exact directory ownership and fresh process identity; preserve the host, foreign processes and all source files. Use one frozen process set and a bounded release, never broad termination or forced handle closure. Recheck ownership and task state across asynchronous reads, reject unknown holders/children/identity drift, and report respawn or incomplete probes without widening the process set. PID reuse must not change the termination target. Remove only the exact empty directory nonrecursively. Preserve original errors and partial progress. Tracker closure still requires physical absence and passing current integration evidence.

### Outcome 1: Supported Windows recovery boundary
- Work: Add a repository-owned Windows adapter and a close-owner recovery helper. Discover the exact process set from native directory handles and approved helper executable/entrypoint identities. Bind process creation, executable, command digest, parent and directory; terminate through retained process objects after revalidation. Keep native desktop release capability distinct from this bounded repository adapter.
- Risks/open questions: An unavailable or incomplete Windows probe, a running task, foreign child, ambiguous helper, target drift or host respawn must stop without inventing ownership. Recovery runs outside the Issue task's directory so it does not reactivate the holder while trying to remove it.
- Verify: Windows fixture processes hold real directories; one recovery releases only the owned fixture and removes the directory, while unrelated processes survive. Exercise rejection before effects and partial-failure evidence.

### Outcome 2: Existing closeout continuation
- Work: Wire the recovery entry into the closeout owner contract and document how the coordinator calls that owner from outside the idle Issue task. Preserve both ordered leases, integration gates and same-task continuation after physical absence. Preserve immutable old packages and evidence.
- Verify: Focused close-continuation, pending-host-cleanup, integration-verification and contract checks. Verify independent Issues and sequential retries do not require a repository concurrency change.

### Outcome 3: Verification and review
- Work: Run focused behavior checks and applicable repository checks, inspect the complete diff, and obtain independent Standards and Spec review for the consequential process boundary. Keep the main checkout and active Issue 80 work unchanged.
- Verify: Passing check results, no confirmed independent review findings, exact file scope and clean baseline attribution. State explicitly whether real Issue 78 cleanup or installed-package adoption was performed; fixture success alone does not prove either.

### Settled diagnosis and decision

- **F1, accepted:** One common cleanup defect across two selected tasks on the local host. The user chose "保留清理要求，新增精確 helper 回收（建議）" in the reply to `call_eGwdEzi1iKvgjKJkQBylzfZ2`. The explicit repair request authorizes implementation beyond the retrospective skill's normal diagnosis-only handoff. The alternative of closing with an empty directory still present was rejected.
- **E78:** `Run issue workflow 78`, task `01a08041-4b0e-7182-882a-95effb70219c`. The original session `rollout-2026-09-08T17-02-38-01a08041-4b0e-7182-882a-95effb70219c.jsonl`, lines 4114 and 4194, records EBUSY after integration and Git deregistration; line 4155 shows a serial retry with the original task idle. Historical package: `74a22e1916f73e4de29b6d9d96e6431ea4f29ff12d8b93f91f37585bc804402e`.
- **E79:** `Run issue workflow 79`, task `01a0807b-be7f-7d82-bf32-07ef587938ce`. The original session `rollout-2026-09-08T18-06-29-01a0807b-be7f-7d82-bf32-07ef587938ce.jsonl`, lines 9455, 9608, 9660 and 9678, records a sharing violation, then the independently authorized recovery of 12 plus 3 helpers and physical removal. Its earlier merge conflict was resolved before this cleanup failure. Historical package: `852bd245fbd94b8a7a815de4a52e036cfa691f97c1169fddff6e604ef53ea90f`.
- **Coverage:** Original logs are under the local Codex `sessions/2026/09/08` directory. The native history response was capped; these exact local sessions supplied the relevant original events. This sample establishes the common cause for the two requested Issues, not a frequency estimate across the latest 50 tasks. No timing savings are inferred.
- **Cause and owner:** Windows helpers retained the unregistered empty worktree as their current directory. Idle task status did not release their handles, so serial retries also failed. At the verified baseline, `pending-host-cleanup.mjs` could assess ownership but only return `HOST_CLEANUP_BLOCKED`; the native bridge still correctly advertised no release API. The close owner now supplies a separate bounded Windows adapter and an outside-task recovery route. Physical absence and integration evidence remain mandatory.

### Verification and delivery evidence

- Focused behavior suite: **8/8 passed** (`windows-helper-recovery`, `pending-host-cleanup`, `close-continuation`). Real Windows fixtures cover exact helper termination, host/unrelated-helper preservation, active-task rejection, foreign-child rejection, new-holder detection between stops and partial outcomes after audit failure.
- After the final owner fixes, the Windows end-to-end test passed again with canonical task-ref retry protection, owner result-audit failure preservation and ordinary directory removal after a prior batch. The affected continuation/document contract checks passed **6/6**.
- Skill contracts: **46/46 passed**. The reading-load regression initially caught extra entry text; moving detail behind the conditional recovery reference restored its bound, and that test passed **1/1**. No full repository suite is claimed.
- Independent Standards and Spec reviews each ended with **0 confirmed findings and 0 advisories**. Both reviewed the final owner SHA256 `5ebbe3d2db67ce3bcfc071917daf8a5c048148f400f2df2957901b9b5c847376`.
- Live tracker read-back: Issue 79 closed at `2026-09-09T02:29:26Z`; Issue 78 closed at `2026-09-09T03:11:25Z`. Original tasks/operators completed that live cleanup. This change did not terminate their helpers or mutate either tracker Issue.
- At the end of implementation, the main checkout remained clean at the baseline and the isolated changes were uncommitted. No source integration, push, global installation or retained-package rewrite had been performed.

### Authorized delivery continuation

- The user's follow-up on 2026-09-09 explicitly authorizes merging the repair and applying it to the installed version. Commit only this repair's 15 reviewed files, then fast-forward the clean `features/ron` checkout under its target mutation writer. Preserve other worktrees; do not push or modify tracker state.
- Reuse the passing behavior checks and independent reviews after verifying unchanged implementation bytes. Verify the delivered target tree against the candidate, then use the existing `install-workflow.mjs` owner to snapshot that exact merged commit into the trusted cache and update the managed `.codex` and `.agents` public workflow entries. Existing repository-linked entries update with the checkout.
- Before installation, verify all retained packages and exact public link targets. Afterwards, verify the new current version, both managed entries, every changed packaged file, retained versions and cleared installer state. Keep the installer's exact link backups. Record command and read-back evidence privately under the Git common directory; do not rewrite previous Grants or package contents.
