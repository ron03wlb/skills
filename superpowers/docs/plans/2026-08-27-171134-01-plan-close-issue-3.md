# Close skills Issue #3

**Goal:** 將 Issue #3 的 exact reviewed candidate 本地整合至 `features/ron`，保留 target dirty state，移除 exact Issue worktree，並關閉及 read-back Issue。
**Why planning is required:** 此 closeout 會修改 target Git history、永久移除一個 worktree，並改變 GitHub Issue state。
**Acceptance:** Latest execution state必須維持為 read-back `implementation_complete` comment `5436837498`，candidate必須是 clean `C:\tmp\skills-issue-3-prerequisite-runtime` 的 exact `HEAD` `808a105334fd6fea44c698adf70ff26b4af29e16`。Integration candidate必須只組合 captured target與candidate且兩者皆可達；candidate delta與完整 target dirty path集合必須零碰撞。整合前後 canonical dirty snapshot與effective post-merge hook evidence必須相同，`PREPARED`與`VERIFIED` receipt都必須逐欄read-back。Target只能從captured `T`以`--ff-only`前進到exact `I`；cleanup只能移除exact clean Issue worktree並保留topic branch。Issue closure必須read-back。任何 identity、blocker、snapshot、hook、tracker或Git mismatch立即停止，不rebaseline、rollback、stash、reset、repair、push或deploy。

### Outcome 1: 固定 closeout identities 與 integration candidate
- Work: Bind Issue #3、execution-state identity `E`、target `T`、candidate `C`、Planning Seal、worktree identities與blocker state；在isolated temporary worktree從exact `T`建立或驗證`I`，不修改reviewed candidate。
- Verify: `C`等於clean registered Issue worktree `HEAD`；`T`與`C`皆為`I` ancestors；temporary worktree clean且內容只來自Git composition。

### Outcome 2: 證明 unrelated target work 可保留後再整合
- Work: NUL-safe snapshot所有staged、unstaged、untracked paths及index/worktree/type/mode fingerprints，解析rename兩端、檢查path-prefix collision並fingerprint effective post-merge hook；append/read-back `dirty-target-preservation:v1` `PREPARED`後重讀所有gate，再從original target worktree fast-forward至`I`。
- Verify: Target仍為exact `T`且execution state/blockers/snapshot/hook未漂移；整合後target `HEAD == I`、`C` reachable、dirty snapshot與hook evidence相同；append/read-back matching `VERIFIED`。

### Outcome 3: Exact cleanup 與 tracker closure
- Work: 在兩次matching `VERIFIED` gate中重讀target、execution state與blockers；移除exact clean registered Issue worktree，保留topic branch，最後關閉Issue。
- Verify: Target仍為`I`且candidate reachable；Issue worktree registration/path消失；topic branch保留；Issue read-back為CLOSED；既有dirty state digest/counts維持；未push、remote-merge或deploy。
