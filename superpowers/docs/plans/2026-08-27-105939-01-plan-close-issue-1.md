# Close skills Issue #1

**Goal:** 將 Issue #1 的 exact reviewed candidate 本地 fast-forward 整合至 `features/ron`，保留 target dirty state，移除 exact Issue worktree，並關閉及 read-back Issue。
**Why planning is required:** 此 closeout 會修改 target Git history、永久移除一個 worktree，並改變 GitHub Issue state。
**Acceptance:** Candidate 必須是 clean `C:\tmp\skills-issue-1` 的 exact `HEAD`，且包含目前 target；candidate delta 與 target dirty paths 必須零碰撞。整合前後 canonical dirty snapshot 必須相同，PREPARED 與 VERIFIED receipt 必須成功 read-back，target 只能 `--ff-only` 到 candidate，cleanup 只能移除 exact clean Issue worktree。Topic branch、recovery checkpoint 與 unrelated files全部保留；Issue closure 必須 read-back。任何 identity、snapshot、tracker 或 Git mismatch 立即停止，不 rollback、stash、reset、push 或 deploy。

### Outcome 1: 固定 closeout identities 與 preservation evidence
- Work: Read back Issue、最新 completion state、target/candidate/worktree identities、ancestry、blockers、dirty snapshot、collision 與 recovery checkpoint。
- Verify: candidate clean且等於 completion SHA；target仍為 recorded baseline；collision為零；snapshot digest及counts可重算。

### Outcome 2: 只做可證明的本地整合
- Work: Publish/read-back PREPARED receipt，重新驗證 target與snapshot未漂移，再由 original target worktree fast-forward到 exact candidate；失敗則保留現況並留下 Issue open。
- Verify: target `HEAD` 等於 candidate；candidate reachable；dirty snapshot未變；VERIFIED receipt逐欄 read-back。

### Outcome 3: Exact cleanup 與 tracker closure
- Work: 在 VERIFIED gate後移除 exact clean Issue worktree、保留 topic branch與 recovery checkpoint，再關閉 Issue。
- Verify: worktree registration/path均不存在；topic/recovery仍存在；Issue state read-back為 closed；未 push。
