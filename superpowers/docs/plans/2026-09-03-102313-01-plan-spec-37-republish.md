# Republish Spec #37: Concurrent Spec lanes and delivery

## Outcome

Republish existing GitHub Issue #37 as one Multi-Issue parent that treats Issue #38 as a completed prerequisite and decomposes the remaining work into four vertical outcomes: isolated Spec planning, simplified producer publication, concurrent per-Spec DAG delivery, and installed end-to-end proof.

## Bound authority

- Tracker: GitHub repository ron03wlb/skills
- Spec: #37 in revision mode
- Target: features/ron
- Planning Seal: 41db4697bf67a6fc43ac301b0a253d621c11475b (successor)
- Delivery classification: Multi-Issue
- Approved scope identity: sha256:4a24bdc031871f8d8ad0e595305c217fc46222b5521859a875fe82b99b33a84d
- Plan path: superpowers/docs/plans/2026-09-03-102313-01-plan-spec-37-republish.md

## Publication sequence

1. Create a new operation-scoped to-spec@v1 checkpoint transaction for this exact revised scope; preserve the prior mismatched progress-zero transaction unchanged.
2. Write and read back only this bound publication plan.
3. Acquire the shared target writer, commit only this plan, read back its exact commit and target state, then release the writer.
4. Invoke attest-target-contribution for that exact checkpoint commit and read back the immutable contribution record.
5. Update existing Issue #37 with the canonical Multi-Issue parent body, preserve ready-for-agent, and read back the exact publication.
6. Append and read back the immutable to-spec handoff, ending only with /to-tickets #37.

## Verification

- The new transaction identity, target, Planning Seal, scope identity, plan path, and generated content identity match this operation.
- The transaction advances only through plan.written, checkpoint.committed, attestation.read_back, publication.read_back, and handoff.completed.
- The plan checkpoint contains only this plan file and descends from the Planning Seal.
- The contribution record owns only the plan checkpoint and is read back from Issue #37.
- The revised Issue remains Multi-Issue, contains no child Acceptance Criteria or Implementation Plan, preserves ready-for-agent, and ends with /to-tickets #37.
- The target is clean at handoff. No children, implementation, Run, integration, aggregate verification, push, deployment, release, or skill installation occurs.

## Exclusions

This operation does not delete or rewrite the earlier checkpoint transaction, create child Issues, implement Issue #37, start a DAG Run, integrate a candidate, verify a push range, push, deploy, release, or mutate unrelated repository or tracker state.
