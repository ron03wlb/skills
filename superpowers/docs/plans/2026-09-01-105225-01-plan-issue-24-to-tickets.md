# Reconcile and publish Issue 24 decomposition

**Goal:** Reconcile Multi-Issue Spec #24 into one complete, source-grounded, dependency-safe child Issue set and publish only its dependency-ready frontier.
**Why planning is required:** The workflow may create GitHub Issues, native parent and blocking relationships, a parent publication comment, and triage-label mutations.
**Acceptance:** Before mutation, verify the configured `ron03wlb/skills` tracker and labels, the complete Issue #24 body and comments, inherited Planning Seal ancestry on `features/ron`, the complete expected Decomposition-key set and canonical child contracts, all tracker-supported identity sources, native relationships, readable External blockers, and an acyclic owned blocker graph. Stop without mutation on duplicates or any conflicting key, parent, target, seal, body, contract, relationship, record, or partial-publication evidence. On publication failure, preserve and report every created identity, exact key mapping, edge state, selected full seal, and failed read-back; never amend, reset, duplicate, or rewrite conflicting tracker evidence. Completion requires exact read-back of every child contract and native relation, one matching `decomposition:v1` parent record, and final ready-label state while preserving pre-existing local work.

### Outcome 1: Freeze planning and decomposition authority

- Work: Bind Issue #24 to target `features/ron`, validate inherited Planning Seal `f189b29705556d489f61f3322429f005c0a250bf`, inspect ADR-0044 through ADR-0048 and the current promoted workflow surfaces, and derive the minimal independently verifiable child contracts without revising approved behavior.
- Risks/open questions: Any new public behavior, Acceptance Criterion, target, exclusion, or ownership decision is a Spec revision and must return to `/to-spec`.
- Verify: `git cat-file -e f189b29705556d489f61f3322429f005c0a250bf^{commit}` and `git merge-base --is-ancestor f189b29705556d489f61f3322429f005c0a250bf features/ron`

### Outcome 2: Complete a mutation-free reconciliation preflight

- Work: Search all supported Decomposition-key identity sources, parent comments, GitHub sub-issue relations, issue dependencies, labels, and recoverable partial-publication evidence for every expected Issue #24 child.
- Risks/open questions: Duplicate keys, unexpected native parents, unexpected blockers, incomplete relationships without a bound partial-publication record, or canonical contract mismatches are hard stop conditions and are never auto-repaired.
- Verify: `gh issue view 24 --repo ron03wlb/skills --comments --json body,comments,labels,state` plus exact GitHub GraphQL read-back of parent, child, and dependency identity sources

### Outcome 3: Publish only missing canonical children and relationships

- Work: Create missing children in blocker-before-dependant order, attach every supported native parent and blocking relation, record created identities immediately, and read each body and relationship back exactly once.
- Risks/open questions: A write or read-back failure leaves recoverable partial publication; stop before parent publication and report the exact successful and failed state.
- Verify: `gh issue view <Issue-ID> --repo ron03wlb/skills --comments --json number,body,state,labels` plus exact GitHub GraphQL sub-issue and dependency queries

### Outcome 4: Publish completeness and the ready frontier

- Work: Append or reuse exactly one matching `decomposition:v1` record on Issue #24, read it back, compute blocker closure from the published graph, apply `ready-for-agent` only to open unblocked children, remove it from blocked or closed children, and read all final states back.
- Risks/open questions: Parent-record conflict or multiple plausible records stops without label changes; label mutation occurs only after successful record read-back.
- Verify: `gh issue view 24 --repo ron03wlb/skills --comments --json comments` and `gh issue view <Issue-ID> --repo ron03wlb/skills --json state,labels`
