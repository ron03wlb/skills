# Close skills Issue #4

**Goal:** Integrate the exact reviewed Issue #4 candidate into the current local `features/ron` target, preserve all unrelated target work, remove only the registered Issue worktree, and close the Issue.
**Why planning is required:** This operation mutates the dirty target branch, removes a worktree, writes tracker receipts, and closes an external Issue.
**Acceptance:** The latest execution state is one valid `implementation_complete` note; blocker #3 remains closed; target `T`, reviewed candidate `C`, integration candidate `I`, Planning Seal, branch, worktree, review, and verification identities agree. A canonical NUL-safe dirty-target and hook snapshot has no collision with `T..I`, is bound to a read-back `dirty-target-preservation:v1` `PREPARED` receipt, and remains identical through integration. The original target advances only through `git merge --ff-only <I>`; both `T` and `C` are ancestors of `I`; a read-back `VERIFIED` receipt proves inclusion and preservation before cleanup and closure. Any conflict, drift, collision, receipt mismatch, failed preservation proof, or third SHA stops with Issue #4 open and without automatic repair or rollback. No push, remote merge, deployment, topic-branch deletion, unrelated-file deletion, product-code repair, or expensive review/full-suite rerun occurs.

### Outcome 1: Pin closeout authority and identities
- Work: Read Issue #4 and ordered execution history once, confirm the latest terminal state is `implementation_complete`, verify blocker #3 remains closed, and bind the current target `T` and clean registered candidate worktree `C` to the recorded identities.
- Verify: Issue state is OPEN; receipt fields, worktree registration, branch, baseline, Planning Seal, candidate, clean reviews, final verification, and clean worktree evidence agree exactly.

### Outcome 2: Prepare an isolated integration candidate
- Work: Create a temporary integration worktree and branch from exact `T`; derive `I` using the ancestry rule without rebasing or editing product content.
- Verify: Both `T` and `C` are ancestors of `I`, the integration tree/parents match the required case, the temporary worktree is clean, and no unexpected delta exists.

### Outcome 3: Prove dirty-target preservation before integration
- Work: NUL-safely snapshot target staged, unstaged, and untracked paths plus effective post-merge hook evidence; compare all path and prefix collisions against `T..I`; publish only digest/counts/hook evidence in a `PREPARED` receipt and read it back.
- Verify: Target remains at `T`, latest execution state and blockers are unchanged, the recomputed snapshot and hook match, and the read-back receipt binds exact `E`, target branch, `T`, `C`, and `I`.

### Outcome 4: Integrate, verify, clean up, and close
- Work: Fast-forward the original target to `I`, recompute preservation evidence, update/read back the receipt as `VERIFIED`, recheck all cleanup gates, remove only the exact clean registered Issue worktree, close Issue #4, and read back closed state once.
- Verify: Target `HEAD == I`, `C` is reachable, preservation digest/counts/hook are unchanged, the `VERIFIED` receipt matches, the Issue worktree is absent, the topic branch remains, Issue #4 is CLOSED, and unrelated target work remains preserved.
