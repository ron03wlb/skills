# Decompose Issue 19 into executable Issues

**Goal:** Reconcile and publish the complete, source-grounded Multi-Issue decomposition for Spec Issue #19, then expose only its dependency-ready frontier.
**Why planning is required:** The workflow may create GitHub Issues, native parent and blocking relationships, a parent publication comment, and triage-label mutations.
**Acceptance:** Before mutation, verify the configured `ron03wlb/skills` tracker and labels, Issue #19 body and comments, inherited Planning Seal ancestry on `features/ron`, the complete expected Decomposition-key set, all existing key matches, native relationships, readable external blockers, and an acyclic owned graph. Stop without mutation on duplicates or any conflicting identity, contract, relationship, target, seal, or partial-publication evidence. On publication failure, retain all created identities and report the exact key mapping, edge state, selected full seal, and failed read-back; never amend, reset, duplicate, or rewrite conflicting tracker evidence. Completion requires read-back of every child contract, native relation, one exact `decomposition:v1` parent record, and final ready-label state while preserving all pre-existing local work.

### Outcome 1: Freeze the canonical decomposition
- Work: Derive independently verifiable vertical slices from Issue #19's approved scope and source evidence in `CONTEXT.md`, relevant ADRs, workflow skills, public docs, metadata, and `tests/ron-workflow/skill-contracts.test.mjs`; bind each criterion to plan and verification coverage.
- Risks/open questions: A child boundary that adds behavior or exclusions beyond Issue #19 requires returning to `/to-spec`; Issue #13 or #14 must not be mutated merely because they provide historical evidence.
- Verify: `git merge-base --is-ancestor 7f0c5d3a5b13e2ec722569efe46d42fb462e19a8 features/ron`

### Outcome 2: Complete the mutation-free publication preflight
- Work: Search all supported Decomposition-key identity sources, parent comments, GitHub sub-issue relations, issue dependencies, labels, and any recoverable partial-publication evidence for Issue #19.
- Risks/open questions: Duplicate keys, unexpected native parents, unexpected blockers, or contract mismatches are hard stop conditions and are never auto-repaired.
- Verify: `gh issue view 19 --repo ron03wlb/skills --comments --json number,body,state,labels,comments,url`

### Outcome 3: Publish and read back missing children and relationships
- Work: Create only missing canonical children in blocker order, then add the supported native sub-issue and dependency relations. Record exact created identities immediately and read every body and relationship back once.
- Risks/open questions: If any write or read-back fails, stop before the parent publication record and report the recoverable partial state.
- Verify: `gh api -H "X-GitHub-Api-Version: 2026-03-10" repos/ron03wlb/skills/issues/19/sub_issues`

### Outcome 4: Publish completeness and synchronize the ready frontier
- Work: Append or reuse exactly one matching `decomposition:v1` record on Issue #19, read it back, compute blocker closure, apply `ready-for-agent` only to open unblocked children, remove it from blocked or closed children, and read all final states back.
- Risks/open questions: No ready-label mutation or `/execute-issue` command is valid until the parent publication record has passed read-back.
- Verify: `gh issue view 19 --repo ron03wlb/skills --comments --json number,state,comments,url`
