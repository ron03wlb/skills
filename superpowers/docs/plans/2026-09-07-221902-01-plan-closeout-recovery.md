# Recover closeout without replacing execution authority

**Goal:** Repair mixed-target reconciliation and recognition of partial Windows worktree removal, then preserve the original Spec 64 continuation.
**Why planning is required:** Runtime closeout decisions affect tracker closure and shared target authority.
**Acceptance:** Preserve original Grants, candidates, reviews, and failed observations. Never weaken exact-head, ownership, cleanliness, or candidate reachability checks. No push, branch deletion, arbitrary helper termination, or bypass of the previously rejected directory deletion. A live policy restriction may remain a separately reported host blocker.

### Outcome 1: Coherent target evidence
- Work: Rebuild reconciliation, including tracker and node facts, when its target HEAD differs from the subsequent target read. Bound retries; persistent drift stops without dispatch or mutation. Keep the reducer's equality check unchanged.
- Verify: Deterministic adapter tests move HEAD between reads, change candidate reachability, and exhaust the read budget. Existing end-to-end checks remain passing.

### Outcome 2: Partial cleanup preserves completion
- Work: Recognize only an exact, ordinary, empty, unregistered Issue directory with its recorded topic at the candidate and candidate target-reachable as pending cleanup. Preserve COMPLETE and PRESENT until physical removal. Nonempty, linked, foreign, moved-topic, missing-registered, and unreachable states remain unresolved. Update the closeout reference owner without enlarging entry instructions.
- Verify: Real temporary Git/worktree reader tests exercise partial removal and negative ownership cases. Tracker closure remains illegal until cleanup completes.

### Outcome 3: Reviewed delivery and bounded continuation
- Work: Run focused and repository checks and independent review required by applicable contracts. Include the exact maintenance reference extracted by Issue 67 in the committed package allowlist/archive; inspection found it missing from the installer. Preserve the separately installed GitLab repair when preparing any compatible installation. Resume only the original Run after all required runtime and host predicates hold; do not replay execution or overwrite completion records. Panel shutdown and terminal shorthand selection are independent follow-up repairs if needed for this continuation.
- Verify: Diff scope, clean candidate, installed package identity/compatibility, original Grant preservation, actual terminal and process exit. Report any unavailable host capability separately from passing fixture tests.
