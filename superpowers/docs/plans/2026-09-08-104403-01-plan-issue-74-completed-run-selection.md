# Reconcile explicitly selected completed Runs

**Goal:** Deliver Issue #74 (Spec #72, decomposition 72/02) by selecting the original completed Run through current canonical identity and approved scope.
**Why planning is required:** High-risk Run authority and cross-owner runtime selection.
**Acceptance:** AC-1 through AC-4 pass at the installed entry with original Grant/journal/candidate evidence preserved, no repeated completed work, unchanged no-argument/STOPPED/scope/package gates, and independent batch reconciliation. Commit only this Issue contribution in its existing worktree; independent Standards and Spec review and completion read-back precede stopping. Integration, closure, installation, push and deployment remain later owners' actions. Preserve evidence and stop on changed authority, ambiguous ownership or unavailable required verification.

Execution baseline and reused Planning Seal: `0dd5111da99b2a719b8085ef76be0c2818e95e1a`. Target: `features/ron` at `C:/Workspace/open_source/skills`. Sole Issue worktree: `C:/Users/ron.chang/.codex/worktrees/1c27/skills`; topic: `codex/issue-74-completed-run-selection`. Operation: `workflow-op-v1-ee2ae2ed6f1c7e3b891bc0c3b5353986a58e77e78b5d2229c541d30abe466810`. The original Run Grant and dispatch were read back before mutation; parent/child digests and empty native blockers agree. Generic host plan/verification skills are authorized by the coordinator clarification; all workflow owners remain pinned to package `db130a91219ef4df4a7339b0a6c3b9149b6fc66692768ce32be656f0529bda37`.

### Outcome 1: Exact completed selection (AC-1, AC-2)
- Work: Exercise and correct `installed-entry.mjs` using numeric/native Spec identity, current body scope, canonical operation identity and original journal authority. Keep no-argument discovery unique and non-terminal; preserve stopped, changed, duplicate and mismatched selection gates.
- Verify: `node --test tests/ron-workflow/installed-entry.test.mjs` with a failing behavioral regression before implementation.

### Outcome 2: Package and batch continuity (AC-1, AC-3, AC-4)
- Work: Pass selected authority through the retained verifier, compatible runtime observation and existing batch/Run owners. Exercise retained/current compatibility, unavailable/modified/incompatible/foreign packages, mixed active/completed lanes, capacity and close ordering. Update the operator guide with actual fixture evidence and its limitations.
- Necessary discovery (AC-1): installed-entry regression and independent review proved that changed close evidence after `SUCCEEDED` could replay close messages, including when a disposable snapshot is missing or a temporary blocker replaces it. The GitHub reconciliation owner reads paginated native closure history against the current exact Spec publication; an OPEN member or parent closed under that publication remains contradictory on every read. Earlier-scope closures do not fence current progress, and unprovable history stays blocked. This changes no scope or authority and adds no receipt or Grant.
- Necessary discovery (AC-3): returning from a compatible current runtime to the verified retained package omitted its actual runtime observation. Compare against the last observed runtime (or original Grant before any transition) so both directions use the existing `runtime.observed` owner.
- Necessary discovery (AC-1, AC-3): independent fixtures using real baseline code proved that both unavailable-current fallback and a verified older current catalog could bypass the newer entry's replay gates. Require the selected verified runtime composition's completed re-entry support before invoking any existing Run, independently of disposable snapshots; unsupported current or fallback code returns package recovery. This internal compatibility check leaves Run/Grant/journal authority unchanged. Repository regression covers that boundary and restored compatible-current success; historical baseline/candidate reproduction remains separate local verification evidence.
- Verify: `node --test tests/ron-workflow/installed-entry.test.mjs tests/ron-workflow/workflow-installation.test.mjs tests/ron-workflow/run-issue-workflow-batch.test.mjs`.

### Outcome 3: Candidate evidence and completion
- Work: Run repository checks on the committed candidate, record configuration/runtime/external-input identity through the pinned verification cache, obtain independent Standards and Spec reviews, repair confirmed in-scope findings within the persistent ten-wave limit, and publish/read back one completion. This plan is the only prospective non-contract workflow artifact.
- Verify: `node --test tests/ron-workflow/*.test.mjs`; `npm run check-plugin-version`; `node --check skills/personal/run-issue-workflow/scripts/installed-entry.mjs`; `git diff --check`; clean worktree and exact reviewed HEAD. No configured typechecking script exists in `package.json`.
