interface:
  display_name: "Run Issue Workflow"
  short_description: "Run one Spec DAG with leaf-owned close leases"
  default_prompt: "Use $run-issue-workflow to complete the selected Spec DAG and report its terminal result or exact blocker."
policy:
  allow_implicit_invocation: false
