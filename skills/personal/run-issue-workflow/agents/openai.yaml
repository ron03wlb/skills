interface:
  display_name: "Run Issue Workflow"
  short_description: "Deliver one Tracker Spec through Codex Issue lanes"
  default_prompt: "Use $run-issue-workflow to reconcile and run this exact Tracker Spec through Codex-native Issue lanes."
policy:
  allow_implicit_invocation: false
