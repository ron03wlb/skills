interface:
  display_name: "Wiki"
  short_description: "Inspect or update a repository Wiki"
  default_prompt: "Use $wiki to inspect or update this repository's Wiki independently from Issue delivery."

policy:
  allow_implicit_invocation: false
