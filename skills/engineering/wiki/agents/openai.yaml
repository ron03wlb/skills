interface:
  display_name: "Wiki"
  short_description: "Initialize or synchronize the Canonical Wiki"
  default_prompt: "Use $wiki to initialize or synchronize this repository's Canonical Wiki from verified state."

policy:
  allow_implicit_invocation: false
