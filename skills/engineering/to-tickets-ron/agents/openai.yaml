interface:
  display_name: "To Tickets Ron"
  short_description: "Create bounded executable Issue contracts"
  default_prompt: "Use $to-tickets-ron to split this Change Spec into dependency-ordered executable Issues."
policy:
  allow_implicit_invocation: false
