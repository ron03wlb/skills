interface:
  display_name: "Verify Target Before Push"
  short_description: "Verify local or pushed exact ranges"
  default_prompt: "Use $verify-target-before-push to select a local-ahead or already-pushed completion-note range, prove contribution coverage, and run one aggregate gate without pushing."

policy:
  allow_implicit_invocation: false
