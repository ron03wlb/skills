interface:
  display_name: "Verify Target Before Push"
  short_description: "Verify ranges and recover narrow evidence"
  default_prompt: "Use $verify-target-before-push to select a local-ahead or already-pushed completion-note range, bind deterministic owner-derived aggregate identities for its Specs, classify workflowArtifacts compatibility from exact preceding Spec-scoped workflow_artifacts_contract_adopted:v1 records without treating them as coverage authority, prove coverage and any Successor verification evidence, and run one aggregate gate where only exact-evidence Confirmed code review findings block while each Code review advisory remains visible; report confirmed findings to a new human-created Aggregate repair Issue without creating or executing it. For eligible direct-contribution coverage recovery, closed Issue evidence reconciliation, or historical command placeholder recovery, require exact human confirmation, invoke only the bounded matching helper, and restart from fresh Entry without pushing."

policy:
  allow_implicit_invocation: false
