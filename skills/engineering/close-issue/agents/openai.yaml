interface:
  display_name: "Close Issue"
  short_description: "Integrate an exact candidate and close one Issue"
  default_prompt: "Use $close-issue to compose one exact reviewed candidate with its current local target, prove inclusion, remove the Issue worktree, and close the Issue."

policy:
  allow_implicit_invocation: false
