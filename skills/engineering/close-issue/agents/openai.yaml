interface:
  display_name: "Close Issue"
  short_description: "Resume an idempotent three-action close"
  default_prompt: "Use $close-issue to merge one unchanged completed candidate into its recorded target, remove its exact clean worktree, and close the Issue, or close a verified Multi-Issue parent."

policy:
  allow_implicit_invocation: false
