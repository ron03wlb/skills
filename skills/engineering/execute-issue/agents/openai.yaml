interface:
  display_name: "Execute Issue"
  short_description: "Execute one human- or DAG-authorized Issue"
  default_prompt: "Use $execute-issue only for a direct human request or an exact Issue authorized by a valid read-back DAG Run Grant; preserve its dedicated-worktree, review, completion-note, and no-close boundaries."
