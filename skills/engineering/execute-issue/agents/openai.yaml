interface:
  display_name: "Execute Issue"
  short_description: "Execute one human- or DAG-authorized Issue"
  default_prompt: "Use $execute-issue only for a direct human request or an exact Issue and Spec authorized by a valid read-back DAG Run Grant; bind its deterministic owner-derived implementation identity; route one exact unresolved prerequisite through pre-execute-issue in the same authorized lane and preserve its content-bound attestation through completion; keep the dedicated-worktree, review, completion-note, and no-close boundaries; repair only exact-evidence Confirmed code review findings while keeping each Code review advisory visible and non-blocking; append or reuse independent exact Spec-scoped workflow_operation_identity_contract_adopted:v1 and workflow_artifacts_contract_adopted:v1 records before prospective completion; and declare exact workflowArtifacts or an explicit empty list without granting coverage authority."
