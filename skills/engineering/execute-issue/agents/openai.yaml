interface:
  display_name: "Execute Issue"
  short_description: "Keep Issue completion valid as target moves"
  default_prompt: "Use $execute-issue to implement and verify one tracker Issue in its recorded-target worktree, preserving a valid completion while concurrent closes advance that target."

policy:
  allow_implicit_invocation: false
