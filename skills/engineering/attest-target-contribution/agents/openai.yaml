interface:
  display_name: "Attest Target Contribution"
  short_description: "Record eligible direct target authority"
  default_prompt: "Use $attest-target-contribution only for an exact human-confirmed coverage recovery packet handed off by /verify-target-before-push or a prospective Workflow plan checkpoint from a frozen legacy/profile-v1 to-spec or to-tickets resume; fresh profile-v2 producers have no such route. Require no second confirmation for that exact checkpoint, append or reuse the unchanged minimal tracker record, return its immutable identity, and grant no readiness."
