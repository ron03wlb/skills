interface:
  display_name: "Pre-Execute Issue"
  short_description: "Record one executed Issue artifact"
  default_prompt: "Use $pre-execute-issue to record that sql/v0.4.20.sql was already executed for Issue #108."

policy:
  allow_implicit_invocation: false
